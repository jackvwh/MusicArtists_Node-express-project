{
  "name": "express-project",
  "version": "1.0.0",
  "license": "ISC",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "(npx live-server ./frontend ) & cd backend && npm start",
    "dev": " (npx live-server ./frontend ) & cd backend && npm run dev",
    "postinstall": "cd backend && npm i"
  },
  "devDependencies": {
    "prettier": "^3.0.3"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "live-server": "^1.2.2"
  }
}
